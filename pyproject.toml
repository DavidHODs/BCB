[project]
name = "BMS"
version = "0.1.0"
description = ""
authors = [
    {name = "DavidHODs",email = "davidoluwatobi41@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "flask (>=3.1.0,<4.0.0)",
    "mypy (>=1.15.0,<2.0.0)",
    "pydantic (>=2.10.6,<3.0.0)",
    "connexion[swagger-ui] (>=3.2.0,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
